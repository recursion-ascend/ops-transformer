[pytest]
markers =
    ci: mark a test as a CI test
    graph: marks tests as graph mode compilation tests
    consistency: marks tests as batch consistency tests (default off, requires SMLA_CONSISTENCY_TEST=1)
